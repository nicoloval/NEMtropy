[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: long-running tests (ensemble sampling, large graphs)
    integration: end-to-end solver tests via graph classes
    math: log-likelihood and numerical function tests
addopts = -m "not slow"
